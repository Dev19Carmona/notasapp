export const useGrades = () => {
  const students = [
    "Camilo Carmona",
    "Cristiano Ronaldo",
  ];

  const initialValues = {};
  const validationSchema = {}
  handleGradesControl = () => {}
  return {
    handleGradesControl,
    validationSchema,
    initialValues,
    students,
  };
};
